{
  "name": "playwright-mcp-ui-test",
  "version": "1.0.0",
  "description": "MCP server for headed browser UI capture and analysis using Playwright",
  "main": "dist/index.js",
  "bin": {
    "playwright-mcp-ui-test": "./dist/index.js",
    "playwright-mcp-ui-test-install": "./dist/install.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "install-browsers": "npx playwright install chromium"
  },
  "keywords": [
    "mcp",
    "playwright",
    "browser-automation",
    "ui-testing",
    "claude"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
